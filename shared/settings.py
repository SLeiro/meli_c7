# Filename to store the dataset generated during the ETL Step
DATASET_FILENAME = "fda-example-train.csv"

# Parameters to split the dataset into sepparate train and test sets
DS_SPLIT_SIZE = 0.1
DS_SPLIT_SEED = 1

SAMPLE = 150000